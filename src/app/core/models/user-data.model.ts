export interface UserData {
  userName: string;
  role: string;
}

export interface UserInfo {
  fullName: string;
  emailId: string;
  photoURL: string;
}

export interface UserTokenModel {
  givenName: string;
  role: string;
  userId: number;
}
